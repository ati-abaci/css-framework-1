import { createTheme } from "@mui/material/styles";

const customTheme = createTheme({
  palette: {
    primary: {
      main: "#a4c7eaff",
    },
    secondary: {
      main: "#cab6ceff",
    },
    success: {
      main: "#afd8b0ff",
    },
  },
});

export default customTheme;
